using System;
using Microsoft.EntityFrameworkCore.Migrations;

#nullable disable

namespace FamilyHub.Infrastructure.Migrations
{
    /// <inheritdoc />
    // Односторонние референсы ("<47"/">47") и нормы из знаний модели (RefSource.Inferred, план
    // "нормы из бланка") — новая активная версия analysis.extract: refLow/refHigh теперь
    // раскладывают и односторонний диапазон, плюс новое поле "refExpected" для строк без
    // референса в бланке вовсе. Версия вычисляется динамически (MAX(Version)+1), не хардкодом —
    // на прогретой БД мог накопиться неизвестный набор версий (в т.ч. от ручных правок админом
    // через POST /api/admin/pipeline/prompts/{key}/versions).
    public partial class AddReferenceRangePrompt : Migration
    {
        private static readonly DateTime SeedCreatedAt = new(2026, 9, 14, 0, 0, 0, DateTimeKind.Utc);

        private static readonly Guid AnalysisExtractPromptId = new("45508844-3607-4c68-b345-b34ca62d67f4");

        private static readonly Guid NewVersionId = new("bf607182-9304-46b6-def0-516c7d8e96a6");

        private const string AnalysisExtractBodyV3 = """
            Ты — оцифровщик бланков лабораторных анализов. На входе — текст или фото бланка анализа
            (может быть только часть бланка, если документ большой). Извлеки ВСЕ показатели, которые
            реально присутствуют в этом фрагменте, и верни ТОЛЬКО валидный JSON, без пояснений, без
            markdown, без блока <think>.

            Формат ответа:
            {
              "indicators": [
                {
                  "name": "название показателя БЕЗ порядкового номера пункта бланка и БЕЗ единицы измерения (например, \"Гемоглобин\", не \"1. Гемоглобин, г/л\")",
                  "value": "значение как напечатано (например, \"118\" или \"отрицательно\")",
                  "unit": "единица измерения или null (например, \"г/л\")",
                  "refLow": 130,
                  "refHigh": 160,
                  "refText": "референсный диапазон текстом или null — заполняй ТОЛЬКО если референс НЕ раскладывается на refLow/refHigh (например, \"отрицательно\", \"1-3 в п/зр\")",
                  "refExpected": "ожидаемый НОРМАЛЬНЫЙ результат этого показателя по общемедицинским знаниям — заполняй ТОЛЬКО если в бланке референса нет вовсе (ни числом, ни текстом); если референс в бланке есть — всегда null"
                }
              ],
              "documentDate": "дата анализа/забора материала, как указана в бланке, в формате YYYY-MM-DD, или null",
              "doctor": "ФИО и/или специальность врача, назначившего анализ, если указаны в бланке — иначе null, не придумывай"
            }

            Правила:
            - Извлекай ТОЛЬКО то, что реально написано в этом фрагменте — ничего не добавляй от себя
              и не переноси показатели из общих знаний о медицине.
            - "name" — название показателя БЕЗ порядкового номера строки/пункта бланка ("1.", "12)" и
              т.п. в начале — это нумерация бланка, не часть названия) и без единицы измерения (она
              отдельным полем "unit"). Регистр — как обычно пишут в литературном тексте (с заглавной
              буквы), даже если в бланке весь текст напечатан КАПСОМ.
            - Если в строке бланка нет значения (пустая ячейка, только название показателя без цифры
              или текста напротив, ЛИБО там стоит только прочерк "-"/"—") — НЕ включай этот показатель
              в ответ вообще, пропусти его: прочерк ничего не говорит о результате анализа, хранить
              его бессмысленно. Если же в бланке явно написано СЛОВОМ "отсутствуют", "не обнаружено"
              или "отрицательно" — это осмысленный качественный РЕЗУЛЬТАТ анализа, а не пустая ячейка:
              включай показатель с ним как есть — для него тоже можно заполнить "refExpected" (см.
              ниже), если референса в бланке нет.
            - "refLow"/"refHigh" — числа, только если референс — диапазон (например, "130-160"), В ТОМ
              ЧИСЛЕ односторонний: "<47"/"до 47"/"менее 47"/"не более 47" → refLow=0, refHigh=47;
              ">47"/"от 47"/"более 47"/"не менее 47" → refLow=47, refHigh оставь null (верхней границы
              нет). "refText" при этом заполни ТОЖЕ — референс как напечатан в бланке буквально (чтобы
              пользователь видел исходную формулировку, не "0-47"). Если референс не раскладывается ни
              на диапазон, ни на такую одностороннюю форму (например, "отрицательно", "1-3 в п/зр") —
              заполни только "refText", "refLow"/"refHigh" оставь null.
            - "refExpected" — заполняй ТОЛЬКО когда в этой строке бланка референса нет ВООБЩЕ (ни
              refLow/refHigh, ни refText) — тогда, если ты уверен, укажи ожидаемый нормальный результат
              по общемедицинским знаниям (например, для теста на инфекцию — "не обнаружено", для
              показателя с типичной нормой — сам диапазон текстом, "3,5-5,0"). Если референс в бланке
              есть в любом виде — "refExpected" всегда null. Не придумывай норму для показателя, в
              котором сам не уверен — лучше null, чем ошибочная подсказка.
            - "documentDate"/"doctor" — заполняй, только если это ДЕЙСТВИТЕЛЬНО есть в этом фрагменте
              (обычно в шапке документа); если фрагмент — просто таблица показателей без шапки,
              оставь оба null.
            - Если во фрагменте нет ни одного показателя анализа (это шапка документа, подпись врача,
              пояснительный текст и т.п.) — indicators пустой массив, но documentDate/doctor всё равно
              заполни, если они есть в этом фрагменте.
            - Верни строго один JSON-объект, ничего кроме него.
            """;

        /// <inheritdoc />
        protected override void Up(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.Sql($"""
                UPDATE "PipelinePromptVersions" SET "IsActive" = false
                WHERE "PromptId" = '{AnalysisExtractPromptId}' AND "IsActive" = true;
                """);

            // Версия — MAX(Version)+1 по факту в БД, не хардкод: на живой БД к этому моменту могли
            // накопиться версии сверх сидированных миграциями (ручные правки админом).
            migrationBuilder.Sql($"""
                INSERT INTO "PipelinePromptVersions" ("Id", "PromptId", "Version", "Body", "IsActive", "Note", "CreatedAt")
                SELECT '{NewVersionId}', '{AnalysisExtractPromptId}', COALESCE(MAX("Version"), 0) + 1,
                       '{Escape(AnalysisExtractBodyV3)}', TRUE,
                       'Односторонний референс ("<47"/">47") раскладывается в refLow/refHigh; новое поле "refExpected" — ожидаемая норма от модели, когда референса в бланке нет вовсе (RefSource.Inferred).',
                       '{SeedCreatedAt:O}'
                FROM "PipelinePromptVersions" WHERE "PromptId" = '{AnalysisExtractPromptId}';
                """);
        }

        /// <inheritdoc />
        protected override void Down(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.Sql($"""
                DELETE FROM "PipelinePromptVersions" WHERE "Id" = '{NewVersionId}';
                """);

            migrationBuilder.Sql($"""
                UPDATE "PipelinePromptVersions" SET "IsActive" = true
                WHERE "PromptId" = '{AnalysisExtractPromptId}' AND "Version" = (
                    SELECT MAX("Version") FROM "PipelinePromptVersions" WHERE "PromptId" = '{AnalysisExtractPromptId}'
                );
                """);
        }

        /// <summary>Экранирование одиночной кавычки для интерполяции в сырой SQL — тело промпта
        /// содержит только двойные кавычки (JSON внутри C#-raw-строки), но экранируем защитно, на
        /// случай будущей правки текста.</summary>
        private static string Escape(string value) => value.Replace("'", "''");
    }
}
